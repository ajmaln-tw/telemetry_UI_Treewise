import { Box, Typography } from "@mui/material";
import React from "react";

const EmissionCalculatorFilter = () => {
    return <Box>
        <Typography>EmissionCalculatorFilter</Typography>
    </Box>;
};

export default EmissionCalculatorFilter;
